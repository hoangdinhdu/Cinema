@model IEnumerable<Cinema.Models.Giochieu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Thông tin giờ chiếu</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ThoiGianChieu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ThoiGianChieu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDGioChieu }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDGioChieu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDGioChieu })
        </td>
    </tr>
}

</table>
