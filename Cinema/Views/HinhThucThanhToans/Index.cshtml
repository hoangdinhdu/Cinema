@model IEnumerable<Cinema.Models.HinhThucThanhToan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LoaiHinhThuc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LoaiHinhThuc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.hinhthucID }) |
            @Html.ActionLink("Details", "Details", new { id=item.hinhthucID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.hinhthucID })
        </td>
    </tr>
}

</table>
